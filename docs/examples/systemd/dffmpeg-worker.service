[Unit]
Description=DFFmpeg Worker Service
After=network.target

[Service]
Type=simple
User=dffmpeg
Group=dffmpeg
# Adjust this path if you installed elsewhere
WorkingDirectory=/opt/dffmpeg/worker
Environment="PATH=/opt/dffmpeg/worker/bin:/usr/local/bin:/usr/bin:/bin"
ExecStart=/opt/dffmpeg/worker/bin/dffmpeg-worker
Restart=on-failure
RestartSec=5s
# Graceful Shutdown Settings
KillMode=process
TimeoutStopSec=1800

[Install]
WantedBy=multi-user.target
